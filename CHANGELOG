v0.1.1

Flash the UI when heart rate data come.
Use `poll` to handle messages, and eliminates data read latency.

v0.1.0

Display heartrate inside the game.
HeartBeatLan datasource is added.
A config UI is added.